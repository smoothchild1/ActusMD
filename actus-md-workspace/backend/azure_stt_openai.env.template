# ----------------------------------------
# Azure secrets template
# ----------------------------------------
# Copy this file to `azure_stt_openai.env` (already gitignored) and fill in
# real values. `src/loadEnv.ts` loads that file explicitly on server start.

# ---- Azure OpenAI (clinical document generation, multimodal) ----
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_VERSION=
AZURE_OPENAI_DEPLOYMENT_NAME=

# ---- Azure AI Speech (ambient dictation) ----
SPEECH_KEY=
SPEECH_REGION=
SPEECH_LANGUAGE=
