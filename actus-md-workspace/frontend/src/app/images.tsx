import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function ImagesScreen() {
  return (
    <PlaceholderScreen
      title="Images"
      blurb="Radiology and clinical photography for the selected patient will live here — thumbnails, viewer, and links back into the charting workflow."
    />
  );
}
