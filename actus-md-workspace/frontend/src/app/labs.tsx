import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function LabsScreen() {
  return (
    <PlaceholderScreen
      title="Labs"
      blurb="Laboratory results and trends for the selected patient will live here — result tables, flags, and time-series once the data feed is connected."
    />
  );
}
