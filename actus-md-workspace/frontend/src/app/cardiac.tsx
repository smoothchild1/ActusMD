import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function CardiacScreen() {
  return (
    <PlaceholderScreen
      title="Cardiac"
      blurb="Cardiology-specific data for the selected patient will live here — ECGs, echo summaries, device interrogations, and the cardiac specialty snapshot."
    />
  );
}
